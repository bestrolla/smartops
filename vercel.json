{
  "installCommand": "cd Admin_SmartOps && npm install --legacy-peer-deps",
  "buildCommand": "cd Admin_SmartOps && npm run build",
  "outputDirectory": "Admin_SmartOps/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
